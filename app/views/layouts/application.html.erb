<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "CrowdFunding" %></title>
  <%= csrf_meta_tags %>


  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= yield :head %>
  <link href="images/favicon.ico" rel="shortcut icon">
  <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
  <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
  <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
</head>
<body>

<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/">CrowdFunding</a>
      <div class="container nav-collapse">
        <ul class="nav">

          <li>
            <a href="/"><%= current_user.role unless current_user.nil? %></a>
          </li>
          <% if user_signed_in? %>
              <% if cannot? :show, User %>
              <li>
                <a href="/"><%= current_user.profile.full_name rescue nil %></a>
              </li>
              <li>
                <% if current_user.profile.present? %>
                    <a href="<%= edit_profile_path(current_user.profile) %>">Edit Profile</a>
                <%else %>
                    <a href="<%= new_profile_path %>">Create Profile</a>
                <% end %>

              </li>
              <li>
                <a href="<%= new_idea_path %>">Propose Idea</a>
              </li>
              <li>
                <a href="<%= ideas_path %>">My Ideas</a>
              </li>
              <li>
                <a href="<%= show_good_idea_ideas_path %>">My Good Ideas</a>
              </li>
              <li>
                <a href="<%= my_own_project_ideas_path %>">My Own Projects</a>
              </li>
                  <% end%>
              <% if  !current_user.nil? and current_user.role == "Local Admin" %>
              <li>
                <a href="<%= user_management_path %>">User Management</a>
              </li>
                <%end  %>
              <% if can? :show_local_admin, "Global Admin" %>
              <li>
                <a href="<%= local_admins_show_local_admin_path %>">Admin Management</a>
              </li>
              <%end  %>
              <% if !current_user.nil? and current_user.role == "Main Local Admin" %>
              <li>
                <a href="<%= main_local_admin_users_from_not_any_country_path %>">User From not any country</a>
              </li>
              <li>
                <a href="<%= main_local_admin_ideas_belonging_to_users_path %>">All Ideas</a>
              </li>
             <%end  %>
        <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container">
  <div class="content">
    <div class="row">
      <div class="span9">
        <div id="user_nav">
          <% [:notice, :error, :alert].each do |level| %>
              <% unless flash[level].blank? %>
                  <div class="<%= flash_class(level) %> fade in">
                    <a href="#" data-dismiss="alert" class="close">×</a>
                    <%= content_tag :p, flash[level] %>
                  </div>
              <% end %>
          <% end %>
        </div>

        <%= yield %>
      </div>
      <div class="span3">
        <%= render partial: 'shared/side_bar' %>
      </div><!--/span-->
    </div><!--/row-->
  </div><!--/content-->

  <footer>
    <p>&copy; Company 2012</p>
  </footer>

</div> <!-- /container -->

<!-- Javascripts
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->


</body>
</html>
