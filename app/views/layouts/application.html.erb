<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "CrowdFunding" %></title>
  <%= csrf_meta_tags %>


  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= yield :head %>
  <link href="images/favicon.ico" rel="shortcut icon">
  <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
  <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
  <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
</head>
<body>

<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/">CrowdFunding</a>
      <div class="container nav-collapse">
        <ul class="nav">
         <li class="brand"> <%=current_user.role if current_user.present? %>   </li>

          <% if user_signed_in? %>
              <% if !current_user.nil?  %>
                  <% if current_user.role == "Global Admin" && request.url.index(ADMIN_HOST).present? %>
                      <%= render :partial => 'shared/global_admin_layout'%>
                  <% end %>
                  <% if current_user.role == "Main Local Admin" && request.url.index(ADMIN_HOST).present? %>
                      <%= render :partial => 'shared/main_local_admin_layout'%>
                  <% end %>
                  <% if current_user.role == "Local Admin" && request.url.index(ADMIN_HOST).present?%>
                      <%= render :partial => 'shared/local_admin_layout'%>
                  <% end %>
                  <% if current_user.role == "User" || current_user.role.blank? %>
                      <%= render :partial => 'shared/user_layout' ,:locals => {:current_user => current_user}%>
                  <% end %>
                  <% if current_user.role == "Admin Group Owner" && request.url.index(ADMIN_HOST).present?%>
                      <%= render :partial => 'shared/admin_group_owner_layout'%>
                  <% end %>
                  <% if current_user.role == "Business Group Owner" && request.url.index(ADMIN_HOST).present?%>
                      <%= render :partial => 'shared/business_group_owner_layout'%>
                  <% end %>
                  <% if current_user.role == "Main Admin Group Owner" && request.url.index(ADMIN_HOST).present?%>
                      <%= render :partial => 'shared/main_admin_group_owner_layout'%>
                  <% end %>
              <%end  %>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container">
  <div class="content">
    <div class="row">
      <div class="span9">
        <div id="user_nav">
          <% [:notice, :error, :alert].each do |level| %>
              <% unless flash[level].blank? %>
                  <div class="<%= flash_class(level) %> fade in">
                    <a href="#" data-dismiss="alert" class="close">×</a>
                    <%= content_tag :p, flash[level] %>
                  </div>
              <% end %>
          <% end %>
        </div>

        <%= yield %>
      </div>
      <div class="span3">
        <%= render partial: 'shared/side_bar' %>
      </div><!--/span-->
    </div><!--/row-->
  </div><!--/content-->

  <footer>
    <%= debug(params) if Rails.env.development? %>
    <p>&copy; Company 2012</p>
  </footer>

</div> <!-- /container -->

<!-- Javascripts
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->


</body>
</html>
